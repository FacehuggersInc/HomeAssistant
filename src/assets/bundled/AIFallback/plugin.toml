[plugin]
name = "AI Fallback"
key = "aifallback"
order = 20
icon = "robot"
readme = "readme.md"

[settings]
path = "src/assets/bundled/AIFallback/settings.json"

# Key NAME only. The value is entered under this plugin's settings and stored
# in .env.
[secrets]
keys = ["OPENAI_API_KEY"]
